import {StyleSheet} from 'react-native';
import {SCREEN_WIDTH} from '../../view/CaptureButton/CaptureButton';

export const styles = StyleSheet.create({
  container: {
    width: SCREEN_WIDTH / 4,
    height: SCREEN_WIDTH / 4,
  },
});

import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  btn: {
    backgroundColor: 'tomato',
    padding: 12,
    borderRadius: 10,
    flexDirection: 'row',
  },
  btn_text: {
    color: 'white',
  },
});
